# spotapi is the whole point of this pod: Spotify's own web endpoints, no client ID, no secret, no
# premium account. Pinned because it is unofficial -- an upgrade is a change to test, not to take.
spotapi==1.2.8

# spotapi imports all three at module load and declares none of them, so an install without them fails
# on `import spotapi` rather than on first use. Nothing here talks to Mongo or Redis: they back session
# savers this pod never constructs, since it never logs in.
pymongo==4.18.0
redis==7.1.0
websockets==17.1

fastapi==0.141.1
uvicorn==0.52.4
